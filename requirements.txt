fastapi==0.115.0
uvicorn[standard]==0.32.0
sqlalchemy==2.0.35
alembic==1.13.3
psycopg2-binary==2.9.9
pydantic==2.9.2
pydantic-settings==2.5.2
email-validator==2.2.0
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
bcrypt==4.0.1
python-multipart==0.0.12
python-dotenv==1.0.1
chromadb==0.5.15
pdfplumber==0.11.4
langchain==0.3.4
langchain-community==0.3.2
langchain-chroma==0.1.4
langchain-google-genai==2.1.5
google-genai>=1.3.0  # used directly by scripts/test_rag.py, not the deployed app

# CPU-only torch — default PyPI wheels on Linux bundle full CUDA support
# (unlike Windows/Mac, which get CPU-only by default). Render's free tier
# has no GPU and only 512MB RAM; the CUDA-inclusive build alone was enough
# to OOM the deploy before the app even started serving requests. This
# also speeds up every future build, local and deployed.
--extra-index-url https://download.pytorch.org/whl/cpu
torch==2.4.1+cpu
sentence-transformers==3.2.0