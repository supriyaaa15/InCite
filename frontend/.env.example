# Local dev default — client.js falls back to this even without a .env file.
# In production (Vercel), set this to your deployed Render backend's URL,
# e.g. https://incite-api.onrender.com
VITE_API_BASE_URL=http://127.0.0.1:8000